; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
upload_port = COM3
monitor_port = COM3
monitor_speed = 115200
board_build.filesystem = littlefs
lib_deps = bblanchon/ArduinoJson@^6.21.5

; テスト用ビルド環境
; LMK値は data/config.test.json を編集して設定すること（本ファイルには書かない）
; ビルド: pio run -e esp32dev-test --target upload
; FS書込: pio run -e esp32dev-test --target uploadfs
[env:esp32dev-test]
extends = env:esp32dev
build_flags =
  -DTEST_CONFIG_PATH='"/config.test.json"'
  -DTEST_BRIDGE_MAC='"08:D1:F9:37:39:C0"'

